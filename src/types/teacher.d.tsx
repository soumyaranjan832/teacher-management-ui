export interface TeacherInfo {
  name: string;
  role: string;
  birthDate: string;
  email: string;
  phone: string;
  address: string;
}

export interface Qualification {
  name: string;
  rate: string;
}